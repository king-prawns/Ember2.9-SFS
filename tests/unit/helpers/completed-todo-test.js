
import { completedTodo } from 'admin-forum-ember/helpers/completed-todo';
import { module, test } from 'qunit';

module('Unit | Helper | completed todo');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = completedTodo([42]);
  assert.ok(result);
});

